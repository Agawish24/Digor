[phases.setup]
nixPkgs = ['nodejs_23', 'pnpm', 'openssl', 'postgresql']
nixLibs = ['zlib', 'openssl', 'stdenv.cc.cc.lib']

[phases.build]
cmds = [
  'pnpm install --no-frozen-lockfile --production=false',
  'npx prisma generate',
  'pnpm run build'
]